content/
bin/
certs/
.vscode/

# vendor/ IS tracked on purpose: the boot-time build and the reproducible-build
# verification both run with GOPROXY=off, so the modules must be in the tree.

# Local build output
gcss-amd64
gcss-arm64
release-*
*.zip
